🧩 1️⃣ environment.yml
(para entornos Conda / Anaconda / Miniconda)

Guarda este archivo en la raíz de tu proyecto como environment.yml.
Luego podés crear el entorno con una sola línea:

conda env create -f environment.yml
conda activate conflictoer

name: conflictoer
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - requests=2.32.3
  - beautifulsoup4=4.12.3
  - feedparser=6.0.11
  - pandas=2.2.3
  - numpy=2.1.3
  - dateparser=1.2.0
  - lxml=5.3.0
  - google-search-results=2.4.2
  - soupsieve=2.6
  - urllib3=2.2.3
  - pathlib
  - pip
  - pip:
      - google-search-results==2.4.2


🚀 Cómo usarlo
conda env create -f environment.yml
conda activate conflictoer
python scrapingersf_historico_api.py
