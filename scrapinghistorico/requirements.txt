📦 requirements.txt
# Requerimientos del proyecto: Relevamiento histórico de conflictos laborales (2023–2025)

# --- Scraping y HTTP ---
requests==2.32.3
beautifulsoup4==4.12.3
feedparser==6.0.11

# --- Data analysis y almacenamiento ---
pandas==2.2.3
numpy==2.1.3

# --- Procesamiento de fechas y texto ---
dateparser==1.2.0

# --- API y servicios externos ---
google-search-results==2.4.2   # Cliente oficial de SerpApi

# --- Utilidades del sistema ---
pathlib==1.0.1

# --- Control de warnings (opcional) ---
urllib3==2.2.3

# --- Compatibilidad y parsing de HTML defectuoso ---
soupsieve==2.6
lxml==5.3.0

---

🚀 Cómo instalar todo
Desde la carpeta raíz del proyecto (donde está tu requirements.txt):
pip install -r requirements.txt
---

💡 Si usás un entorno virtual (recomendado):
python -m venv venv
venv\Scripts\activate        # en Windows
# o
source venv/bin/activate     # en Linux / Mac
pip install -r requirements.txt

---
🧠 Verificación rápida
Podés probar que todo esté correcto ejecutando:
python -m pip check

---

Deberías ver:
No broken requirements found.

Y luego ejecutar tu script principal:
python scrapingersf_historico_api.py (nombre de tu archivo)
